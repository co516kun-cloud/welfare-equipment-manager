# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# 上にあった「Debug/test scripts with hardcoded credentials」11本は
# 2026-09-07 に実ファイルごと削除したので、無視指定も不要になった。

# Claude Code のローカル設定（2026-08-23 追加）
# 🔴 このリポジトリは public。ローカルの絶対パスとツール許可設定が
#    初回コミット 2dec216 から追跡されていた。追跡を外した。
#    ⚠️ 履歴からは消えていない（過去のコミットには残る）。
.claude/

# 脆弱性診断レポート（public リポなので追跡しない）
.vuln-scan/

# デモモードのスクリーンショット出力
demo-shots/

# 生成物（install-task.ps1 が書く・PC 固有のパス入り）
scripts/print-agent/launch.vbs
